public class SoftDrink extends Product{
    public SoftDrink(name: String, usedup: boolean,price: double,quantity: double,ExpiryDate: int){
        super(name, usedup, price, quantity, ExpiryDate);
                        }
}
